# vi
*.swp
*.log
*.err
*.tar
*.tgz

ruins

metal/*.node
metal/build/

# Local MLX source checkout (optional dev aid for reading MLX internals;
# the production build links against system MLX, e.g. Homebrew).
mlx/

node_modules/
pnpm-lock.yaml

# Python
__pycache__/
*.pyc
.venv/
.env
env/
test.sh
test/

# Jupyter
.ipynb_checkpoints/

# Project-level shared runtime DB and its backups (churning binary).
runtime.sqlite
runtime.sqlite.backup-*

# Ephemeral pipe working dirs — nothing under pipes/ is retained in git.
# Durable, project-owned content lives at the repo root: config/ (recipes not
# in node_modules) and scripts/ (scripts not in node_modules), both tracked.
# Pipe-local scripts/overrides/data/state are override/debug + run artifacts.

# Local data & outputs
build/
artifacts/
data/
data_report.json
eval_out/
out/
logs/
run*/
dist/
TrainTiny/
params/
state/
pipeline.json
experiment.yaml
override.yaml
*-wal
*-shm
pipes/sample
pipes/story/build
pipes/story/diary

# macOS
.DS_Store

pipeline: story

generate_diary_with_adapter_ite:
  depends_on:
    - build_diary_prompt_ite
  llm:
    maxTokens: 5000
    temperature: 0.7

generate_diary_without_adapter_ite:
  depends_on:
    - generate_diary_with_adapter_ite